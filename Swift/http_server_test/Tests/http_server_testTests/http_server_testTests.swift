import XCTest
@testable import http_server_test

final class http_server_testTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(http_server_test().text, "Hello, World!")
    }
}
